#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Vérifier que lint-staged existe
if command -v npx >/dev/null 2>&1; then
  npx lint-staged || exit 0
else
  echo "⚠️ npx non trouvé, lint-staged ignoré"
  exit 0
fi